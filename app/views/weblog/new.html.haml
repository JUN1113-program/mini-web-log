.flex-contener
  .create-area
  = form_for @log do
    = f.text_field :title, class:"create-area__title",placeholder: "タイトルを書いてね"
    %textarea.create-area__content(placeholder = "本文を書いてね")
    %input.create-area__submit(type = "submit" value = "SEND")
  .side-bar
    サイドバーです